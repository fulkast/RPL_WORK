note
	description: "Plays and stops sounds by the id."
	author: "Rusakov Andrey"
	date: "18.09.2013"

deferred class
	SOUND_PLAYER

inherit
	ACTUATOR

feature -- Access

	start_sound (sound_id: INTEGER_16)
			-- Start playing sound with the given `sound_id'.
		deferred
		end

	start_system_sound (sound_id: INTEGER_16)
			-- Start playing sound with the given `sound_id'.
		deferred
		end

	stop_sound
			-- Stop playing sounds.
		deferred
		end
end
